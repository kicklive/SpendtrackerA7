import { ValidatecurrencyDirective } from './validatecurrency.directive';

describe('ValidatecurrencyDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidatecurrencyDirective();
    expect(directive).toBeTruthy();
  });
});
